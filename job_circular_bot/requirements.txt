requests==2.32.3
beautifulsoup4==4.12.3
feedparser==6.0.11
PyYAML==6.0.2

# Playwright is only needed for sources with parser: "js_render". It is a large
# download (a real browser), so it lives in requirements-browser.txt and the
# workflow installs it only when such a source is configured.
