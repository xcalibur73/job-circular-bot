# Bot state: the "already posted" database and run logs. Kept out of the repo
# and persisted between scheduled runs with actions/cache instead.
job_circular_bot/data/

# Local secrets and virtual environments
.env
.venv/
venv/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Local agent and tooling directories, which must never be published
.agents/
federation/
AGENTS.md
CLAUDE.md
GEMINI.md
skills-lock.json
